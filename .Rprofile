options("width" = 100)
options(tibble.print_max = Inf)
options(repos = c(duke = "http://archive.linux.duke.edu/cran/",
                  cran = "http://cran.us.r-project.org"))

.First <- function() {
    if (interactive()) {
        library(magrittr)
        library(devtools)
    }
}
